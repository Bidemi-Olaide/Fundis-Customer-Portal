{
  "name": "fundis-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run:staging": "cypress run --env configFile=staging",
    "cy:open:staging": "cypress open --env configFile=staging",
    "cy:run:production": "cypress run --env configFile=production",
    "cy:open:production": "cypress open --env configFile=production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.7.3",
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.3",
    "@cypress/webpack-preprocessor": "^6.0.1",
    "cypress-fill-command": "^1.0.2"
  }
}
